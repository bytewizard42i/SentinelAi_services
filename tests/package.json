{
  "name": "sentinelai-tests",
  "version": "1.0.0",
  "description": "Test suite for SentinelAI smart contracts",
  "scripts": {
    "test": "mocha tests/contract-tests/*.test.js --timeout 10000",
    "test:watch": "mocha tests/contract-tests/*.test.js --watch --timeout 10000",
    "test:coverage": "nyc --reporter=html --reporter=text mocha tests/contract-tests/*.test.js"
  },
  "devDependencies": {
    "chai": "^4.3.10",
    "chai-as-promised": "^7.1.1",
    "ethers": "^5.7.2",
    "mocha": "^10.2.0",
    "nyc": "^15.1.0"
  }
}
